# Changelog
2026-06-25 v2.8.0: Best-coding-platform sprint — 9 tested features, lifting the repo from 0 automated tests to 9 passing. Closed Claude-Code/Cursor parity gaps, each shipped WITH an automated test: (1) first-class git tools `git_status/diff/add/commit/log` (was a read-only widget + raw run_command); (2) real **plan/edit/autonomous** operating modes — the `auto_approve` toggle was never enforced; plan mode now intercepts mutating tools (`is_mutating_tool`) and returns "[PLAN MODE] Proposed (not executed)" so the agent proposes without touching files; (3) **verify/test-and-self-correct** loop — `run_tests` auto-detects cargo/go/pytest/npm, the system prompt enforces write→test→fix; (4) **slash commands** `/test /commit /diff /log /review /plan /edit /auto /help`, dispatched before the LLM via `handle_slash`; (5) `run_lint` (clippy/go vet/ruff/eslint); (6) **@-file mentions** — type `@path` to inject a file's contents into context (Cursor-style, `extract_mentions`/`resolve_mentions`); (7) **session checkpoints** — snapshot/restore conversation + touched-file state via `/api/checkpoint|checkpoints|restore`, beyond per-file undo; (8) `multi_edit` — atomic all-or-nothing multi find/replace to one file (MultiEdit parity, pure `apply_multi_edit`); (9) `run_format` (cargo fmt/gofmt/ruff format/prettier) completing the **format→lint→test** quality gate. Tests: git_tools_roundtrip, mode_gate_classification, test/lint/format_command_detection, slash_command_parsing, mention_extraction, checkpoint_snapshot_restore, multi_edit_atomic. cargo test: 9 passed.
2026-05-29 v2.7.0: UI parity with Amni-Ai + cross-app launch. Added the 7 Amni-scient themes (Jarvis, Scient, Clean Dark, Clean Light, Nebula, Terminal, Solar) as full-palette themes (`AMNI_THEMES` → `applyAmniTheme()` sets the whole CSS-var set inline, persisted to `localStorage.amniTheme`, applied on load) on top of the existing accent cycler. New visible **≡ MENU** button in the header opens the command palette, which now also lists every theme + a **Cycle Amni-scient Theme** entry, so modes/themes are discoverable without keyboard shortcuts. New **Open Amni-AI (Adam) ↗** command opens Amni-Ai's UI at `:7700/jarvis` (and nudges the managed server to start) — reciprocal to Amni-Ai's `↗ Open Amni-Code` launcher (`POST /launch/peer`), so you can hop between the two apps from either. Frontend-only (static/index.html, embedded — needs `cargo build` to ship). cargo check: PASS (2 pre-existing dead-code warnings unchanged).
2026-05-29 v2.6.0: Adam is now the PRIMARY brain. `Config::default()` previously fell back to xAI Grok (and the welcome screen said "xAI Grok by default") — it now defaults to the `amni` provider (local granite-based Adam, `adam:granite-gf17`) out of the box, no API key required. Cloud providers (xai/openai/anthropic/google/…) are opt-in via the UI selector (persisted to `~/.amni/config.json`, so existing users keep their explicit choice) or an explicit `AMNI_CODE_PROVIDER=<name>` env override. The `amni` provider's default model is `adam:granite-gf17` (matches Amni-Ai openai_compat `_MODEL_NAME`), and the server health-check no longer brittle-fails on an exact model-string mismatch (reachable = ready for the single local Adam brain). De-hardcoded generation caps: `max_tokens` (was fixed 4096) → `AMNI_CODE_MAX_TOKENS`, agent iteration cap (was fixed 100) → `AMNI_CODE_MAX_ITERS`. Welcome-hint copy updated to "(Adam / Amni-AI local by default)". cargo check: PASS (2 pre-existing dead-code warnings unchanged).
2026-05-25 v2.5.0: Inline widget rendering from Amni-Ai v6.10+. When Adam emits `amni_widgets[]` in `/v1/chat/completions` responses (weather/system/time/news/stock/code/file/git/disk/watch/error/info), the Rust backend now extracts them from `raw_msg.amni_widgets`, emits a new `widget` SSE event per item, and the frontend renders neat themed cards inline at the bottom of the assistant message. Cards adopt the active Amni-Code theme (pink/crypt/haven/etc) via CSS custom properties — no neon mismatch. 12 widget types rendered (weather temp, system grid, time card, news links, stock with colored ▲/▼ change, file mono preview, git branch+commits, generic info/error). Cargo check: PASS (2 pre-existing dead-code warnings unchanged).
2026-05-21 v2.4.0: Full Adam integration. Amni-Code now drives Amni-Ai's Adam (GF(17) texture-native 2B) as a first-class autonomous brain via the new `/v1/chat/completions` endpoint on Amni-Ai v6.9.4. The `amni_base_url()` normalizer extended to auto-remap stale `:8001` and `:8002` saved URLs to the canonical `:7700` (in addition to the existing `:8787` mapping), so users with prior installs auto-migrate without manual reconfig. Adam exposes the same 10 OpenAI tool-call functions Amni-Code already speaks (read_file, write_file, edit_file, run_command, list_directory, search_files, web_fetch, web_search, memory_read, memory_write), and persists successful (intent → tool-sequence) chains in a PTEX cell-address LUT so cross-session coding autonomy improves with use. No frontend or wire-shape changes needed on the Amni-Code side beyond the migration regex — the existing OpenAI tool-call client just works. cargo check passes (2 pre-existing dead-code warnings unchanged). Backups at backups/v2.4.0/.
2026-04-20 v2.2.3: Fixed Amni default startup flow. Provider `amni` now targets the Amni-AI web server on `http://127.0.0.1:7700`, stale saved `8787` URLs are normalized, initial config load triggers server startup immediately for `amni`/`local`, and the frontend hint text now matches the real API port. Backups created, guardians approved, cargo check/build passed.
2026-04-20 v2.2.2: Added direct Google Gemini API support for latest models (gemini-1.5-pro, gemini-1.5-flash, gemini-3.1+). Special handling in llm_request for Google's generateContent endpoint. Backups created, guardians approved.
2026-04-20 v2.2.1: Fixed Ollama import error by changing API call to use "from" field with canonicalized path for GGUF files. Backups created, guardians convened, cargo check passed.
2025-07-26 v2.2.0: i18n + Split Panes + Cross-Platform Installers + Rust WebGL Installer. i18n: 10 languages (en/es/fr/de/ja/zh/ko/pt/ar/ru) via inline I18N dict, t(key) function, data-i18n/data-i18n-placeholder/data-i18n-title attributes, language selector in settings, RTL support for Arabic (dir="rtl"). Split panes: up to 3 concurrent independent agent sessions, draggable dividers (min 280px), per-pane model selector, paneSend() with separate SSE connections, "Split View" in command palette. Installers: WiX XML for Windows MSI (perUser, PATH env, Start Menu + desktop shortcuts), macOS DMG (build-dmg.sh, .app bundle, hdiutil), Linux .deb (build-deb.sh, DEBIAN/control, /usr/local/bin, .desktop), build-all.sh wrapper. install_gui.py upgraded to v2.2.0: model picker with checkboxes, PATH setup via winreg/shell rc, OS detection. Backups at backups/v2.2.0/.
2025-07-09 v2.1.0: Product accent theme system from amni-scient-site — 8 accent themes (Code/pink, Crypt/blue, Haven/purple, AI/amber, Core/red, Explore/cyan, Calc/orange, Green/mint) with dark+light variants, cycle button in header, persisted to localStorage. Grid background overlay, backdrop blur on header, pulse/float keyframe animations. VS Code parity features: Status bar (bottom bar with language, cursor position, version), Toast notifications (animated popups for save/actions), Quick Open (Ctrl+P fuzzy file picker), Breadcrumbs (file path segments above editor), enhanced command palette (+Quick Open, +Cycle Accent). Scan-line effect tinted to match accent color.
2025-07-09 v2.0.0: Major IDE upgrade — Multi-file tabs (open/close/switch files with separate Monaco models + view state preservation), Command Palette (Ctrl+Shift+P with fuzzy search, 14 commands), File watcher (SSE /api/fs-events auto-refreshes sidebar on changes), Web tools (web_fetch + web_search with SSRF protection), Per-project instructions (.amni-instructions.md / .github/copilot-instructions.md / AGENTS.md loaded into system prompt), Persistent memory (memory_read/memory_write tools, /api/memory endpoint), CLI CWD auto-set (first arg directory becomes working dir). Added url+regex crate deps. Frontend: tab bar with close buttons, command overlay with arrow-key navigation. Backend: handle_fs_events SSE handler, handle_memory_list JSON endpoint, load_custom_instructions, load/save_memory_store, is_private_url SSRF guard, strip_html, exec_web_fetch/exec_web_search.
2026-04-05 v2.3.0: run.bat now auto git pull+build-if-changes (incremental cargo), 'amni' cmd safe. v2.2 cwd fixed. No cmts, concise, backups+checklist, guardians majority (Wakka for incremental build).
2025-07-19 v1.0.0 COMPLETE REWRITE: Unified Rust-only backend with full agent loop (15-iteration tool-calling), 6 tools (read_file, write_file, edit_file, run_command, list_directory, search_files), multi-provider LLM support (Ollama/OpenAI/Anthropic/xAI), session management, auto-browser-open. New self-contained single-file embedded UI with dark theme, code-change diff visualizer, settings panel with auto-approve toggle, onboarding welcome screen. Eliminated Python/Flask/Node/React dependencies — single binary, one double-click run.bat that auto-installs Rust and builds.
2026-03-23 v0.1.5 Simple Mode: added toggle for Ollama/Claude-code simple UI (hides code preview for cleaner chat). Keys auto from PATH/env/hf_token. Distributable README. Ternary mode logic.2026-03-23 v0.1.4 Dropdown UX: model now dropdown with easy presets (grok-beta, llama3.2 etc), API key hides for non-API via ternary, much better UX no typing needed. Updated App.tsx.2026-03-23 v0.1.3 Model+Key+Provider+Enter: updated frontend for custom model input, api key field, provider select (Grok API/Local Ollama/Mock) to support users w/o keys using local models, Enter sends request (no shift), backend ternary logic for provider handling. Web Grok web version not directly hooked (brittle/TOS issues, recommend API or Ollama).2026-03-23 v0.1.2 Combined Launch + Backend: added server.py (Flask 8001 /api/execute), run.bat for single-button backend+frontend, updated launcher with code-all, fixed fetch to 8001, no port conflicts. Updated docs.2026-03-23 v0.1.0 Grok Computer Use Agent: added full computer-use like Claude with live edits, terminal, git, ssh stubs, guardrails toggle, prompt buttons, model select (grok-beta). Extended Amni-Code frontend and tools. Reused Amni-Ai patterns. Architecture updated. Guardians approved with majority for safety. No comments, concise ternary code. Tested terminal integration.2026-03-23 v0.1.1 Launcher Integration: added Amni-Code to launcher for easy launch (dev server) and build. Updated launcher server.py with new project entry, icon, and launches.
2024-10-04 IDE Prototype: Added Monaco editor pane (toggleable via new icon in header), editor-col with monaco CDN loader, save integration stub, toggleEditor/initMonacoEditor functions. Added persistent sidebar file explorer. Updated architecture_map.md, checklist_ide_upgrade.md and changelog. Completed core IDE uplift - Monaco + tree for real editing experience. (Rikku approved by guardians majority vote - full task done)
2026-03-29 v2.1 AI Suggestions: Guardian proposals for suggestions pane, shortcuts, diagnostics; extended editor JS w/ ternary/concise/async; backups+checklist created; arch+changelog+docs updated; cargo check passed; no comments/dups; structure maintained. Ready for user test.